{
  "name": "@mc/sync-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mission Control Sync Worker — Obsidian two-way sync, ADR generation, repo polling (Phase 2, TDS 02 §2.2)",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:int": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@mc/shared": "workspace:*",
    "drizzle-orm": "0.45.2",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "@types/pg": "8.21.0",
    "pg-boss": "12.27.0",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
