import { AgentTeamsPage } from '../features/agents/teams/AgentTeamsPage.js';

/** `/agents/teams` — agent teams (PRD §5.7, TDS 06 §6.2's reserved third tab). */
export function Component() {
  return <AgentTeamsPage />;
}
