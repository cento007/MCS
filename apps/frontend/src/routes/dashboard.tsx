import { DashboardPage } from '../features/dashboard/DashboardPage.js';

/** `/` — Dashboard home (PRD §8.1, TDS 06 §5.2). One chunk per top-level route (§2.3). */
export function Component() {
  return <DashboardPage />;
}
