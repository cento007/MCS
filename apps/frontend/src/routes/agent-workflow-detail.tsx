import { WorkflowPage } from '../features/agents/workflows/WorkflowPage.js';

/** `/agents/workflows/:workflowId` — one chain, its runs, and the button that starts one. */
export function Component() {
  return <WorkflowPage />;
}
