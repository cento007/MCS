import { SessionsListPage } from '../features/sessions/SessionsListPage.js';

/** `/sessions` — Sessions list, filtered by project, F7 state and managed/observed type. */
export function Component() {
  return <SessionsListPage />;
}
